import React from "react";
import { StyleSheet, Text } from "react-native";
import { SIZE } from "../constants/styles";

export default () => <Text>no results</Text>;

const styles = StyleSheet.create({
    container: {
        marginHorizontal: SIZE.MEDIUM,
    },
});
